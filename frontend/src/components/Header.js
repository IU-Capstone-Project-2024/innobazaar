import React from 'react';

const Header = () => (
  <header className="header">
    <div className="logo">innobazaar</div>
    <input type="text" placeholder="Search for anything..." />
    <div className="login">Login</div>
  </header>
);

export default Header;